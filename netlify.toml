[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# كل يوم اثنين الساعة 08:00 بالتوقيت العالمي.
[functions."weekly-content"]
  schedule = "0 8 * * 1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
